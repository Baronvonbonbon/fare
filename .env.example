# Copy to .env — .env is gitignored. NEVER commit private keys.
DEPLOYER_PRIVATE_KEY=
# Comma-separated extra funded accounts for seed scripts (optional)
TESTNET_ACCOUNTS=
TESTNET_RPC=https://eth-rpc-testnet.polkadot.io/
