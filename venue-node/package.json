{
  "name": "fare-venue-node",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "FARE venue appliance — starts with a gasless relay (sponsor gas + relay settlement).",
  "scripts": {
    "relay": "node --env-file=.env relay.mjs"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  }
}
